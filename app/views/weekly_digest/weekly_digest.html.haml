!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
  %body
    %h1
      Hi, #{@name}
    %p Here's what your teammates have been up to this week:

		- @project.updates.where("created_at >= ? AND created_at <= ?", Date.today.beginning_of_day - 7.days, Date.today.end_of_day).limit(20).each do |update|
			%p #{update.body}
			%p by #{update.person}, posted at #{update.created_at.strftime("%A %d %B")}