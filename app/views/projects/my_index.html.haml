%h1 Your projects

- if @projects.empty?
	%p <span class="logotype">offbot</span> doesn't know about any of your projects yet.
	%p= link_to 'Tell <span class="logotype">offbot</span> about a new project', new_project_path
	
- else
	#pop_box
		%table#project_list
			- @projects.each do |project|
				%tr
					%td= link_to project.name, project
					%td= link_to 'Edit', edit_project_path(project)
					%td= link_to 'Delete', project, :confirm => 'Are you sure?', :method => :delete

	%p= link_to 'New Project', new_project_path
